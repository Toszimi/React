import Vue from 'vue'
import App from './App.vue'
import tablePagination from './components/table_Pagination/index'
const components = [
  tablePagination
]

var comOBJ = {}
comOBJ.install = (Vue) => {
  components.map(key => {
    Vue.component(components[key].name, components[key]) //注册组件
    comOBJ[components[keys].name] = components[key]
  })
}
/**支持标签的引入方式 */
if (typeof window != 'undefined' && window.Vue) {
  install(window.Vue)
}

export default comOBJ
export {
  tablePagination
}

Vue.config.productionTip = false

new Vue({
  render: h => h(App),
}).$mount('#app')